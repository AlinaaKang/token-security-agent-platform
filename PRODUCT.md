# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是参加深信服 AI 安全比赛的项目演示者、评委和安全分析员。他们需要从一句安全目标开始，理解智能体正在做什么、为什么这样做、用了哪些真实或仿真数据，以及结果能证明和不能证明什么。

## Product Purpose

Token Security 是面向大模型应用安全事件的可解释自主调查与处置智能体。它将 Prompt、Token、PCAP、离线安全知识、报告与平台内部处置能力组织成可恢复任务，从发现异常候选到形成证据、调整计划、验证处置和生成报告。

## Positioning

产品不是单一检测页面的集合，而是由公开计划、类型化工具、观察驱动重规划、多假设反证、来源真实性和处置后独立验证组成的安全案件工作台。用户可以检查完整的结构化审查链，同时系统不公开提示词、私人数据或模型内部临时草稿。

## Operating Context

用户通过自然语言创建 Prompt 调查、PCAP 数据集调查、单文件调查或内置跨域演示案件。PCAP 内容只在无网络、只读、非 root、资源受限的 Docker 路径读取。官方比赛平台不可用且赛事方已批准不接入；本地 Web、AutoDL 模型服务和 Docker 是当前演示环境。

## Capabilities and Constraints

- 真实能力包括 Prompt 语义与 Token 检测、PCAP 隔离分诊、知识检索和平台内部工具回执。
- 端点、身份和日志尚无真实连接器，使用明确标注的版本化仿真数据。
- 每个计划最多 12 步、3 个并发工具、2 次重规划和 5 个活跃假设；PCAP 每个执行批次最多 20 个文件，但任务可在授权范围内继续下一批。
- 真实、仿真和派生证据必须始终分开；检测失败、未命中、异常候选和攻击成功是不同状态。
- 只有明确授权才能读取 Prompt/PCAP 或执行内部动作，只有明确取消才停止后台任务。
- Token 侦探挑战的评分和调查规则、冻结检测算法与阈值不得修改。

## Brand Commitments

产品名称为 Token Security 安全智能体。界面采用克制、专业、自然的安全分析师语气。全站使用通透浅蓝导航和蓝色智能体图标；结构化审查链统一使用蓝色，红、绿和琥珀分别只表达异常、成功和失败或待复核。整体可参考 Codex 的工作台结构，但不得照搬其配色。

## Evidence on Hand

仓库已有 Prompt/Token 检测、PCAP Docker 检测与分诊、离线知识、评测中心、攻防实验舱、Token 侦探挑战和旧 SuperAgent API。内置跨域演示数据位于 `data/demo/security-agent-cases.json`，不应被描述为真实 EDR、身份或 SIEM 联动。

## Product Principles

- 从目标到结果的每一步都可观察、可恢复、可解释。
- 结论必须引用证据，并同时展示反证、不确定性、失败和未覆盖范围。
- 真实能力与仿真边界比视觉上的“自动化感”更重要。
- 自动执行受代码策略、授权范围和处置后验证约束。
- 专业工作区可以独立使用，但统一案件工作台负责跨能力编排。

## Accessibility & Inclusion

主要操作支持键盘焦点；移动端关键控件至少 44px；状态不能只靠颜色表达；界面适配减少动态效果设置，并避免长中文、长文件名和窄屏水平溢出。
