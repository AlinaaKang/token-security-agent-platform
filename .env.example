# Disabled by default. Enable only when the base workflow is configured.
TOKEN_SECURITY_LAB_ENABLED=false

# Disabled by default. Enable only for quarantined PCAP batch triage.
TOKEN_SECURITY_PCAP_ENABLED=false
TOKEN_SECURITY_PCAP_QUARANTINE_ROOT=
TOKEN_SECURITY_PCAP_POWERSHELL_EXECUTABLE=
