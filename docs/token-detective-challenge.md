# Token 侦探挑战

## 2026-09-06 页面引导验收

- 挑战按设置、调查、作答、揭晓四个阶段显示独立引导；提交前页面不展示系统动作、系统证据关系或系统 CPD 起点。
- 曲线区固定提示选择最早开始持续变化的位置，而不是最高点；选中位置显示“已选择 Token #N”。
- 完整前端回归为 31 个测试文件、270 个用例通过；生产构建成功，1628 个模块完成转换。
- `1440x900` 与 `390x844` 浏览器检查均无全局横向溢出、控制台错误或页面错误；手机首步浮窗不遮挡“进入挑战”。

`/challenge` 是 AI 安全攻防实验舱的游戏化入口。它复用真实的语义 Guard、Entropy-CPD、固定融合、反事实检查和知识证据结果，但不改变 `/lab` 专业调查流程，也不新增后端、模型、数据库或遥测。

## 如何使用

首次进入页面会显示分阶段浮窗引导；完成或跳过后，可点击右下角“本页引导”重新打开。引导只解释当前可见区域，不会进入挑战、执行调查或提交答案。

1. 保持 AutoDL API 隧道映射到本机 `127.0.0.1:18000`。
2. 在 `frontend` 目录运行 `npm.cmd run dev -- --host 127.0.0.1 --port 5174 --strictPort`。
3. 打开 `http://127.0.0.1:5174/challenge`。
4. 选择“三关速战”或“五关完整挑战”，并在进入前选择“互动调查”或“自动演示”，再点击“进入挑战”。
5. 每关开始后，进度下方首先显示“本关待检输入”。经审核的合成安全样本完整展示；受保护关卡只显示静态攻击家族级说明和 `[对抗攻击内容已隐藏]`。
6. 互动调查中按 Guard 语义侦探、CPD 曲线侦探、Agent 小队队长的顺序点击角色。首次汇报会逐行回放脱敏证据，完成后才提示下一位；已完成角色可随时点击回看。自动演示则按 700ms 阶段回放展示，也可立即跳过。
7. 队长只总结语义安全等级与 CPD 分布候选的一致、分歧或证据不足；提交研判前不会展示系统动作。依据已展示证据选择证据关系、处置动作和适用时的 CPD 起点。标记起点时观察红色 CPD 累积线，选择“最早开始持续变化的位置”，而不是曲线最高点。
8. 提交后查看玩家答案、系统原始动作、逐项得分、反事实敏感性和调查阶段摘要。

刷新页面或点击“退出挑战”会清空本次挑战。进度、答案、分数和连击只存在 React 内存中，不写入 localStorage、sessionStorage、Cookie、IndexedDB 或事件数据库。

## 关卡定义

- 三关速战：普通无害、无害格式突变、AutoDAN。
- 五关完整挑战：普通无害、无害格式突变、GCG、AutoDAN、AdvPrompter。

当前挑战只声明 GCG、AutoDAN 和 AdvPrompter 三类受保护优化攻击，不声明直接危险、BEAST 或 AutoDAN-HGA 挑战覆盖。若完整模式缺少任一攻击族，只禁用五关模式；实验舱或 GPU 不可用时，设置页显示固定降级状态。

## 评分规则

每关按当前系统结果确定性计分：

- 处置动作：50 分。`sanitize_recheck` 在玩家分类中按“人工复核”计分，但揭晓保留系统原始标签。
- 证据关系：20 分。四类为双路正常、仅语义风险、仅分布异常、双路风险。
- Token 起点：30 分。与 CPD 起点距离 0-3 得 30 分，4-8 得 15 分，9 以上或未选择得 0 分。

不存在 CPD 起点时，定位项不适用；语义或 CPD 证据不可用时，证据关系项不适用。每关分数按适用分值归一化到百分制，总分是所有已完成关卡百分制分数的算术平均值。

挑战得分只表示玩家与当前系统输出的一致程度，不是检测准确率、攻击覆盖率、召回率、F1 或 CPD 定位准确率。

揭晓页的“如何理解本关得分”说明动作 50 分、证据 20 分和定位 30 分；不适用项目会从本关可用分值中移除，再归一化为百分制。

## 侦探小队与回放

- Guard 语义侦探：对应语义安全等级和类别。
- CPD 曲线侦探：对应 Token 观测、Entropy/NLL 和变化点候选。
- Agent 小队队长：只总结语义安全等级与 CPD 分布候选的一致、分歧或证据不足，不在提交前泄露系统动作。

互动调查要求玩家依次点击 Guard、CPD 和队长。首次点击后，中央证据台以 420ms 的界面间隔逐行回放由已返回 `LabRunResult` 确定性映射出的脱敏证据；完整汇报结束后才解锁下一位。已完成角色保持可点击，回看会立即显示完整报告，不会重新检测、推进或倒退流程。队长完成后才解锁答题区。

三名公仔始终保持原有站位，只有当前等待点击的角色原地短跳，并显示下一步文字提示。自动演示在接口完成后按返回的 `LabStage` 顺序每 700ms 切换当前角色。420ms 和 700ms 都只是界面展示间隔，不是模型耗时。两种方式展示的都是已完成结果，不是实时流式执行、并行推理、隐藏思维链或因果证明；页面显示的阶段耗时始终采用服务端 `latency_ms`。减少动态效果模式会取消弹跳、庆祝和过渡，逐行报告直接完整显示，但保留文字、图标与键盘回看能力。

互动调查是结构化证据展示，不等于已完成 SuperAgent 自主闭环。

## 隐私边界

挑战运行固定发送：

```json
{
  "scenario_kind": "frozen",
  "sample_id": "<场景 ID>",
  "mode": "analysis"
}
```

当前 `LabRunResult` 不包含 Prompt 或 Token 文本。受保护样本不向浏览器返回、渲染、持久化、记录或截图以下字段：`prompt`、`suffix`、`token_text`、`token_id`、`query_text`、`raw_output`、`guard_raw_output`；受保护场景只展示经审核的静态家族级说明和 `[对抗攻击内容已隐藏]`。不论关卡类型，真实攻击输入、攻击 suffix、Token 文本或 ID、原始模型输出和隐藏推理都不会到达浏览器。截图只使用合成无害场景或受保护样本的静态说明，不包含攻击原文。

“本关待检输入”只提供教学语境：它不证明攻击覆盖，也不参与检测或评分；挑战得分仍只表示玩家与当前系统输出的一致程度。

## 失败与边界情况

- 本关请求失败：显示固定“本关调查失败”，不显示异常正文，不记录答案或分数，可重试同一关。
- 知识阶段不可用：不阻断基础证据评分，报告使用已有确定性结果。
- 无 CPD 起点：页面显示“定位不适用”，不会强制选择 Token。
- 证据关系不可用：该项不进入适用分值。
- 刷新或退出：清空内存会话，不产生排行榜或历史记录。

## 2-3 分钟演示

1. 在设置页介绍三名侦探和两种关卡组，强调挑战分数不是检测性能。
2. 进入三关速战，展示普通无害请求的检测结果回放、双路正常、放行和无起点归一化计分。
3. 展示无害格式突变，说明“分布变化不自动等于恶意”，语义证据用于降低误判。
4. 展示 AutoDAN 受保护样本，只说明 sample ID、三条信号、CPD 候选、起点、融合动作和反事实敏感性，不展示原文。
5. 切回设置页指出五关模式增加 GCG 与 AdvPrompter，最后展示隐私边界与真实验收数据。

## 2026-08-28 验证记录

- 静态检查：`git diff --check` 退出码为 0；仅报告工作副本 LF/CRLF 转换提示，无 whitespace error。
- 前端：`npm.cmd test -- --no-file-parallelism` 为 `10 passed` 测试文件、`120 passed` 测试；`npm.cmd run build` 成功，`1607 modules transformed`。
- 结构和适配：互动流程测试确认三个角色始终存在，中央证据台与“本关线索”是独立区域；390px CSS 将证据字段重排为两列、证据台内边距为 14px。减少动态效果规则覆盖角色、图像容器、角色控制和证据台，且不使用 `display: none` 隐藏核心内容。
- 浏览器：控制器在本轮 CSS 修复前完成 `1440x900` 与 `390x844` 挑战流程验收：无挑战页全局横向溢出，自动回放与跳过正常；`prefers-reduced-motion=reduce` 下角色、图像容器、角色控制和证据台的 animation 为 `none`、transition 为 `0s`、transform 为 `none`；无控制台、页面或 API 资源错误。`/analyze` 横向溢出与既有 56px 基线一致，`/lab` 保持 0。CSS 修复后的有界确认同样通过：挑战页无全局溢出或错误，自动回放与减少动态效果正常，`/analyze` 仍与基线一致，`/lab` 无变化。

这些结果是当前部署和冻结样本的功能验收，不替代独立测试集上的准确率、召回率或 F1。

## 2026-08-29 角色审计回放验收

- 前端：`16 passed` 测试文件、`170 passed` 测试；生产构建成功，`1614 modules transformed`。
- 后端：`515 passed, 1 skipped`；跳过项为未配置 `TOKEN_SECURITY_GPU_TEST_MODEL` 的显式 GPU 集成测试。
- 真实浏览器：桌面互动、移动端减少动态效果、自动演示与四入口共 `3 passed`；控制台错误为 0。
- 固定站位：桌面完整互动和回看过程中，三名公仔最大横向漂移为 `0px`；仅当前等待点击角色原地短跳。
- 移动端：`390x844` 无全局横向溢出；减少动态效果下公仔、图像、提示和审计行的动画均为 `none`。
- 隐私：逐行证据不包含 Prompt、攻击 suffix、Token 文本、词表 Token ID、原始模型输出或隐藏思维链；本地模型路径只显示末级公开模型名。

## 2026-08-29 公开输入任务卡验证记录

- 前端：`npm.cmd test -- --no-file-parallelism` 为 `19 passed` 测试文件、`199 passed` 测试；`npm.cmd run build` 成功，`1617 modules transformed`。
- 仓库检查：`scripts/verify_lab_privacy.ps1` 返回 `privacy_verification=passed`，禁用字段命中、受保护跟踪路径、JSON 错误、SQLite 违规和 API 违规均为 0；`git diff --check` 无 whitespace error。
- 后端：当前工作树 `backend` 位于 `PYTHONPATH` 时，`pytest -q` 为 `520 passed, 1 skipped in 64.01s`；唯一跳过项为未配置 `TOKEN_SECURITY_GPU_TEST_MODEL` 的 GPU 集成测试。
- 在线部署后，健康端点返回 200；服务随后报告 `lab.ready=false`、`lab.reason=disabled`，场景目录返回 503。因此未执行依赖场景目录的桌面 `1440x900`、移动 `390x844`、请求计数和未受影响路由浏览器验收。

## 2026-08-29 公开输入任务卡复验

- 生产 API：健康依赖均为 ready；场景目录返回 200 和 5 个场景。`scripts/verify_lab_privacy.ps1 -BaseUrl http://127.0.0.1:18000` 返回 `privacy_verification=passed`，共 13 个 API 请求，API 违规、禁用字段命中、JSON 错误和 SQLite 违规均为 0。
- 浏览器桌面 `1440x900`：互动调查完成三关流程、关卡切换、退出后重入、受保护卡固定提示和自动演示首关卡验证；正常流程中无控制台、页面或资源错误。互动流程观察到 4 个 Lab Run 请求（含重入首关），任务卡交互本身未增加请求；自动演示首关为 1 个 Lab Run 请求。
- 浏览器移动 `390x844`：文档根节点宽度等于视口宽度，任务卡不溢出，首位侦探控制可点击；正常流程无控制台、页面或资源错误。
- 重试：用浏览器路由拦截模拟两次 500，任务卡在重试前后保持不变；两条控制台资源错误只来自该刻意拦截的失败响应，未计入正常流程错误。
- 未受影响路由：`/analyze`、`/lab` 和 `/super-agent` 均加载至既有就绪状态，未产生新的浏览器错误。

## 2026-08-29 多行安全输入复验

- 浏览器在三关速战推进至无害格式突变关后，桌面 `1440x900` 的完整公开输入卡存在 `<pre>`、其 `textContent` 含至少一个换行符、计算 `white-space` 为 `pre-wrap`；卡片和 `<pre>` 的 `scrollWidth` 均不大于 `clientWidth`，文档根节点宽度为 `1440/1440`。
- 移动 `390x844` 复测同一张卡：`<pre>` 仍含换行且 `white-space` 为 `pre-wrap`；卡片和 `<pre>` 无横向溢出，文档根节点宽度为 `390/390`。本次浏览器错误计数为 0。

## 2026-08-29 评审修复复验一

- 场景目录共 5 项：`synthetic_safe` 的 disclosure 等于 `full`，AutoDAN 的 disclosure 等于 `redacted`，AutoDAN 固定隐藏提示精确匹配，受保护场景禁用字段命中为 0。
- 正常桌面流程的 Lab Run 累计计数按事件为：初始进入 1、第一次推进 2、第二次推进 3、退出后仍为 3、重入 4、自动模式进入 5。每次任务卡渲染后的计数均不增加；`/api/v1/analyze` 始终为 0。正常流程控制台、页面和失败资源错误均为 0。
- 重试夹具拦截了 2 次预期的 500 请求，服务端 Lab Run 为 0，卡片保持不变；夹具产生 2 条预期控制台资源错误，页面错误和失败资源事件为 0。
- 移动 `390x844` 互动模式中，Guard、CPD、Agent 三个按钮均在启用后成功点击并进入选中状态，线索区可见且答案可提交。文档宽度为 `390/390`，卡片宽度为 `358/358`，Lab Run 为 1、analyze 为 0，正常错误为 0。

## 2026-08-29 评审修复复验二

- 目录字段：`synthetic_safe.disclosure == "full" -> true`；`AutoDAN.disclosure == "redacted" -> true`；`AutoDAN.redaction_notice == "[对抗攻击内容已隐藏]" -> true`；受保护场景禁用字段命中为 `0`。

| 正常流程事件 | Lab Run 前后 | Delta | `/api/v1/analyze` |
| --- | --- | ---: | ---: |
| 初始进入 | `0 -> 1` | `+1` | `0` |
| 初始任务卡渲染 | `1 -> 1` | `0` | `0` |
| 第一次推进 | `1 -> 2` | `+1` | `0` |
| 第一次推进后任务卡渲染 | `2 -> 2` | `0` | `0` |
| 第二次推进 | `2 -> 3` | `+1` | `0` |
| 第二次推进后任务卡渲染 | `3 -> 3` | `0` | `0` |
| 退出 | `3 -> 3` | `0` | `0` |
| 重入 | `3 -> 4` | `+1` | `0` |
| 自动模式进入 | `4 -> 5` | `+1` | `0` |
| 自动模式任务卡渲染 | `5 -> 5` | `0` | `0` |

- 重试夹具：浏览器尝试的 Lab 请求依次为 `0 -> 1`、`1 -> 2`，两次均在到达服务端前被拦截，因此服务端 Lab Run 始终为 `0`；任务卡渲染 delta 为 `0`，`/api/v1/analyze` 为 `0`。两条资源错误均来自刻意的 500 夹具，除此之外正常页面和控制台错误为 `0`。
